# ============================================
# REQUIRED - These must be set for the app to work
# ============================================

# Database Connection (REQUIRED)
# Local MongoDB: mongodb://localhost:27017/auth-app
# MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/auth-app
MONGODB_URI=

# JWT Secret (REQUIRED)
# Generate a secure random string (at least 32 characters)
# You can use: openssl rand -base64 32
JWT_SECRET=

# Session Secret (OPTIONAL - defaults to JWT_SECRET if not set)
# Generate a secure random string (at least 32 characters)
# Recommended to use a different secret from JWT_SECRET
SESSION_SECRET=

# Frontend URL (REQUIRED)
FRONTEND_URL=http://localhost:3000

# ============================================
# OPTIONAL - Server Configuration
# ============================================

PORT=5000
NODE_ENV=development
JWT_EXPIRES_IN=7d

# ============================================
# OPTIONAL - Google OAuth (leave empty if not using)
# ============================================

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:5000/api/auth/google/callback

# ============================================
# OPTIONAL - Email Configuration (leave empty if not using)
# ============================================

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=noreply@yourapp.com

# ============================================
# OPTIONAL - Security Configuration
# ============================================

# Password reset token expiry in milliseconds (default: 3600000 = 1 hour)
RESET_TOKEN_EXPIRY_MS=3600000

# Allowed origins for CORS (comma-separated, optional)
# Example: https://example.com,https://www.example.com
ALLOWED_ORIGINS=
